import React from 'react';

/**
 * Display information on the ticket
 * @param {props} props Information that will be displayed
 * @returns {void} Information on the ticket
 */
function Content(props) {
    return (
        <div></div>
    );
}

export default Content;
